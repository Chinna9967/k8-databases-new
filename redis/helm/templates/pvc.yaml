apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ebs-redis-pvc
  namespace: roboshop
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: "roboshop-dynamic-ebs"  # used in dynamic provisioning